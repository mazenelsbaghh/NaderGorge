import type { LiveSupportAdminDashboard } from '@/services/live-support-service';
export function LiveOperationsBoard({ dashboard }: { dashboard: LiveSupportAdminDashboard }) { const oldest = dashboard.conversations.filter(x=>x.status==='Waiting').sort((a,b)=>a.createdAt.localeCompare(b.createdAt))[0]; return <section aria-label="العمليات الآن" className="grid gap-3 sm:grid-cols-4">{[['في الطابور',dashboard.waitingCount],['محادثات جارية',dashboard.activeCount],['أُغلقت اليوم',dashboard.closedToday],['أقدم انتظار',oldest?new Date(oldest.createdAt).toLocaleTimeString('ar-EG',{hour:'2-digit',minute:'2-digit'}):'—']].map(([label,value])=><div key={String(label)} className="rounded-2xl border border-slate-200 bg-white p-4"><p className="text-xs text-slate-500">{label}</p><p className="mt-1 text-2xl font-black text-slate-900">{value}</p></div>)}</section>; }
