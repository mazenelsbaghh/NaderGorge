import { Star } from 'lucide-react';
import type { LiveSupportStaffPerformance } from '@/services/live-support-service';
export function StaffPerformancePanel({ staff }: { staff: LiveSupportStaffPerformance[] }) { return <section className="rounded-3xl border border-slate-200 bg-white p-5"><h2 className="mb-4 font-bold">أداء الموظفين والتقييمات</h2><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{staff.map(item=><article key={item.staffUserId} className="rounded-2xl border p-4"><div className="flex justify-between"><strong>{item.staffName}</strong><span className="flex gap-1 text-amber-700"><Star size={15}/>{item.averageRating?.toFixed(1)||'—'}</span></div><p className="mt-2 text-xs text-slate-500">شارك في {item.participatedConversations} · أغلق {item.closedConversations} · {item.ratingCount} تقييم</p></article>)}</div></section>; }
