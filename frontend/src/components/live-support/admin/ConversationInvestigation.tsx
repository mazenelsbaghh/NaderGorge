'use client';
import { X } from 'lucide-react';
import type { LiveSupportConversationTimeline } from '@/services/live-support-service';
export function ConversationInvestigation({ timeline, close }: { timeline: LiveSupportConversationTimeline; close: () => void }) { return <div className="fixed inset-0 z-[100] grid place-items-center bg-slate-950/60 p-4" onClick={close}><section role="dialog" aria-modal="true" aria-label="تفاصيل المحادثة" onClick={e=>e.stopPropagation()} className="max-h-[90dvh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-white p-5" dir="rtl"><header className="mb-4 flex justify-between"><h2 className="font-bold">سجل محادثة {timeline.conversation.participantName}</h2><button onClick={close} aria-label="إغلاق" className="grid size-11 place-items-center"><X/></button></header><ol className="space-y-3">{timeline.items.map((item,index)=><li key={`${item.at}-${index}`} className="rounded-2xl border p-3"><strong>{item.summary}</strong><time className="float-left text-xs text-slate-500">{new Date(item.at).toLocaleString('ar-EG')}</time><p className="mt-1 text-xs">بواسطة: {item.actorName||'النظام'}</p>{item.safeDetails&&<pre className="mt-2 whitespace-pre-wrap break-words rounded-lg bg-slate-50 p-2 text-xs">{item.safeDetails}</pre>}</li>)}</ol></section></div>; }
