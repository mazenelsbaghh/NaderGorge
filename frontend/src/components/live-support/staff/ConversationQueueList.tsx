import type { LiveSupportConversation } from '@/services/live-support-service';
export function ConversationQueueList({ conversations, selectedId, onSelect, waitingCount }: { conversations: LiveSupportConversation[]; selectedId?: string; onSelect: (item: LiveSupportConversation) => void; waitingCount: number }) { return <aside aria-label="المحادثات المسندة والطابور" className="border-b border-slate-200 bg-slate-50 p-3 lg:border-b-0 lg:border-l"><h2 className="px-2 py-3 font-bold text-slate-900">محادثاتي ({conversations.length})</h2><p className="px-2 pb-3 text-xs text-slate-500">بالانتظار: {waitingCount} — التوزيع تلقائي</p><div className="space-y-2" role="listbox">{conversations.map((item)=><button key={item.id} role="option" aria-selected={selectedId===item.id} type="button" onClick={()=>onSelect(item)} className={`w-full rounded-2xl border p-3 text-right focus-visible:outline-2 focus-visible:outline-cyan-700 ${selectedId===item.id?'border-cyan-600 bg-cyan-50':'border-slate-200 bg-white'}`}><span className="flex justify-between gap-2"><strong>{item.subject||'محادثة دعم'}</strong><small>{item.status}</small></span><span className="mt-1 block text-xs text-slate-500">{item.participantType==='Guest'?'زائر':'طالب مسجل'}</span></button>)}</div></aside>; }
