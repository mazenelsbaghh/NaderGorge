interface:
  display_name: "Spec Kit All"
  short_description: "Refine intent, then run the full Spec Kit workflow"
  default_prompt: "Use $speckit-all to refine my feature request through focused Arabic questions, confirm a clear Feature Brief, then run the full Spec Kit workflow with deep planning, strict tasks, guards, and final tests."
