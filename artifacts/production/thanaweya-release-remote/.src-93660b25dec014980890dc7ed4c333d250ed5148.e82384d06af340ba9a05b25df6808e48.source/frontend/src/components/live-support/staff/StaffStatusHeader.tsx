import type { LiveSupportStaffBootstrap } from '@/services/live-support-service';
export function StaffStatusHeader({ state, connected }: { state: LiveSupportStaffBootstrap; connected: boolean }) { const items = [{ label:'الحضور', value:state.isCheckedIn?'مسجل حضور':'غير مسجل', good:state.isCheckedIn },{ label:'حالة الاتصال',value:connected?'متصل':'يعيد الاتصال',good:connected},{ label:'الحمل الحالي',value:`${state.activeLoad} / ${state.capacity}`,good:state.activeLoad<state.capacity},{ label:'في الطابور',value:String(state.waitingCount),good:state.waitingCount===0 }]; return <header aria-label="حالة موظف الدعم" className="grid gap-3 sm:grid-cols-4">{items.map((item)=><div key={item.label} className="rounded-2xl border border-slate-200 bg-white p-4"><p className="text-xs text-slate-500">{item.label}</p><p className={`mt-1 font-bold ${item.good?'text-emerald-700':'text-amber-800'}`}>{item.value}</p></div>)}</header>; }
