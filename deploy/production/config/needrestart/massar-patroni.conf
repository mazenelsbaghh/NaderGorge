# Keep the PostgreSQL leader under the cluster's controlled maintenance workflow.
# An unattended package upgrade must not restart Patroni on its own.
$nrconf{override_rc}{qr(^patroni\.service$)} = 0;
