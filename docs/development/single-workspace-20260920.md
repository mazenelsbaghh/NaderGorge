# تجميع المشروع في مجلد واحد — 20 سبتمبر 2026

مجلد العمل الوحيد هو `/Users/mazenelsbagh/mazen mac/apps/nader gorge`.

## النتيجة

- أزيلت 42 نسخة عمل إضافية بعد حفظ تاريخها وملفاتها المنفردة. أصبح `git worktree list` يعرض المجلد الأساسي فقط، ولم تعد توجد مجلدات Massar/Nader إضافية بجواره أو في مسارات نسخ العمل التي جرى حصرها.
- حُذفت مهارة `massar-startup` من `.agents/skills` ومن مهارات Codex الشخصية. استُبدل شرط المزامنة قبل التعديل بتعليمات العمل في المجلد الأساسي والمحافظة على التغييرات المحلية.
- جُمعت نسخة الإنتاج `16a3be02` مع العمل المحلي، ثم أضيف تنفيذ لعبة MIM المكتمل من `5aaec753` وتعديلات معاينة استرداد قيمة المحتوى المستخدم من نسخة `refund-usage-preview-v5`.
- إصلاح عرض تقدم الطالب موجود بالفعل ضمن نسخة الإنتاج المستخدمة. لم تُستبدل إصلاحات الإنتاج الأحدث بتعديلات أقدم أو بملفات تحتوي علامات دمج غير محلولة.
- حُدثت قوائم واجهات التطبيق والصلاحيات من المصدر المدموج، ومنها قائمة المسارات المستخرجة من تشغيل التطبيق في الاختبار.
- نُقل مجلد أدلة محرر الامتحانات إلى `artifacts/consolidation-20260920/exam-editor-evidence` داخل المشروع.

## حفظ العمل والاسترجاع

الأرشيف المحلي الخاص موجود في `.git/consolidation-20260920/`. يحتوي `inventory.json` على مسار كل نسخة ومعرّفها ومرجع لقطة المصدر المحفوظة. ويحتوي `removed-directories.json` على المجلدات الـ42 التي أزيلت.

حُفظت 43 لقطة مصدر تشمل المجلد الأساسي، مع مراجع Git للتاريخ القديم. داخل مجلد كل نسخة توجد فروق الملفات وحالة الفهرس وملفات العمل المحلية المضغوطة. تحققت مطابقة الملفات الإضافية المحفوظة ببصمات SHA-256 قبل الحذف. ملفات الاعتماد ونتائج البناء القابلة لإعادة الإنشاء ليست جزءًا من أرشيف الملفات الإضافية.

مراجع الحفظ تبدأ بـ `refs/consolidation/20260920/`. هذه مراجع وأرشيفات داخل المشروع نفسه، وليست نسخ تطوير إضافية. لا تُنشر هذه الأرشيفات؛ قد تحتوي ملفات إعدادات محلية خاصة. لم تُحذف الفروع التاريخية.

التجميع ظاهر في ملفات المجلد الأساسي كتغييرات محلية، مع لقطة نهائية محفوظة بمرجع `refs/consolidation/20260920/final`. لم يُستبدل تاريخ الفرع الأصلي أو فهرسه، ولم تُنشر التغييرات على GitHub أو السيرفرات.

## التحقق

- بناء حل الخادم: ناجح. اختبارات التطبيق: 1498 ناجحة، و18 متخطاة لاحتياجها إلى قاعدة بيانات اختبار.
- فحوص الواجهة المحددة في `make verify-frontend`، وفحص ESLint وبناء Next.js: ناجحة.
- بناء العامل واختباراته: ناجحة، 204 اختبارات.
- عقد واجهة لعبة MIM: 8 اختبارات ناجحة.
- استخراج قائمة مسارات التطبيق أثناء التشغيل: 3 اختبارات ناجحة باستخدام أسرار مؤقتة للاختبار.
- قوائم واجهات التطبيق وقدرات الأدمن: 16 اختبارًا ناجحًا بعد إعادة التوليد. إعادة التوليد لا تعني تفعيل وكيل الأدمن؛ حالة التفعيل بقيت محجوبة.
- إعداد Docker Compose: اجتاز التحقق.
- عقود الأداء: 5 اختبارات Node و26 اختبار Python ناجحة. بوابة أدلة أداء النشر لم تجتز؛ رفضت القياسات القديمة برسالة `source changed after performance measurement`. لم تُغيّر الأدلة لتجاوز هذا الشرط.
- فحص المسافات والتعارضات في الفروق: ناجح.

سجلات التحقق محفوظة في الأرشيف المحلي نفسه بملفات `verify-*.log`. لا يمثل هذا التحقق اعتماد نشر؛ اختبارات قاعدة البيانات وقياسات الأداء اللازمة للنشر لم تكتمل.

## الطلب السابق

مشكلات الاسترداد، وانتظار تصحيح امتحان الطالبة، وإظهار الخرائط الذهنية لم يُنشر لها إصلاح خلال هذا التجميع. التشخيص السابق يربط ظهور الخرائط بإعداد `chapterAids`، ويشير إلى فشل بعض وظائف التصحيح الآلي دون إثبات ارتباطها بالطالبة المذكورة. أهلية استرداد الطالبة في الصورة تحتاج مراجعة سجل الشراء الفعلي.
