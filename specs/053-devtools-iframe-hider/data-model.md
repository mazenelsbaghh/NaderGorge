# Data Model: devtools-iframe-hider

No database or entity model changes are required for this feature.
State is maintained transiently within the memory of the client's browser (e.g. `var _devtoolsOpen = false`).
