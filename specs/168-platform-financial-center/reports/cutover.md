# Cutover checkpoint

The cockpit is read-only by default during rollout. Mutation routes are permission-protected and feature-flagged. The production release completed with all three nodes healthy; final mutation cutover remains an owner-controlled switch after live reconciliation.
